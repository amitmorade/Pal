{
    "name": "pal",
    "displayName": "pal",
    "description": "",
    "version": "0.0.1",
    "engines": {
        "vscode": "^1.93.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onStartupFinished",
        "onCommand:extension.imageClicked",
        "onDidOpenTextDocument"
    ],
    "main": "./extension.js",
    "contributes": {
        "commands": [
            {
                "command": "extension.imageClicked",
                "title": "Image Clicked"
            }
        ]
    },
    "scripts": {
        "lint": "eslint .",
        "pretest": "npm run lint",
        "test": "vscode-test",
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "@types/vscode": "^1.93.0",
        "@types/mocha": "^10.0.8",
        "@types/node": "20.x",
        "eslint": "^9.11.1",
        "@vscode/test-cli": "^0.0.10",
        "@vscode/test-electron": "^2.4.1",
        "typescript": "^4.0.3",
        "vscode": "^1.1.37",
        "vscode-test": "^1.4.0"
    }
}